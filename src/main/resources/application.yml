server:
  port: 8080

spring:
  datasource:
    name: igrsiot
    url: jdbc:mysql://localhost:3306/igrsiot?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.demo.springboot.model
